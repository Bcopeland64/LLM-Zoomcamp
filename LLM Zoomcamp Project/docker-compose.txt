version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    depends_on:
      - db

  db:
    image: sqlite:latest
    volumes:
      - ./data:/data

  dashboard:
    build: .
    command: python src/monitoring/dashboard.py
    ports:
      - "8050:8050"
    volumes:
      - ./data:/app/data
    depends_on:
      - db

volumes:
  data:
